# Trigger only when this workload or the shared Python template changes.
# Keeping the path filter tight reduces unnecessary CI runs.
trigger:
  batch: true
  branches:
    include:
      - main
  paths:
    include:
      - src/workloads/training_pipeline/elt/**
      - src/workloads/training_pipeline/utils/**
      - src/workloads/training_pipeline/main.py
      - src/workloads/training_pipeline/tests/test_elt.py
      - src/workloads/training_pipeline/requirements-ci.txt
      - azure-pipelines/ci/ci-elt.yaml
      - azure-pipelines/templates/python-ci.yaml

# PR validation should mirror the main branch trigger behavior.
pr:
  branches:
    include:
      - main

pool:
  vmImage: ubuntu-24.04

# Variable groups keep secrets and environment-specific values out of YAML.
variables:
  - group: elt-ci-vars

steps:
  # Absolute path from repo root ensures the template is always found.
  - template: /azure-pipelines/templates/python-ci.yaml
    parameters:
      serviceDirectory: 'src/workloads/training_pipeline'
      pythonVersion: '3.14'
      requirementsFile: 'src/workloads/training_pipeline/requirements-ci.txt'
      devRequirementsFile: 'src/workloads/training_pipeline/requirements-ci.txt'
      includeDocker: false