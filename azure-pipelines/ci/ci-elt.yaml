# Trigger only when this workload or the shared Python CI template changes.
# The path filter is intentionally tight so unrelated repo edits do not burn
# CI minutes.

trigger:
  batch: true
  branches:
    include:
      - main
  paths:
    include:
      - src/workloads/training_pipeline/elt/**
      - src/workloads/training_pipeline/utils/**
      - src/workloads/training_pipeline/main.py
      - src/workloads/training_pipeline/tests/test_elt.py
      - src/workloads/training_pipeline/requirements-ci.txt
      - azure-pipelines/ci/ci-elt.yaml
      - azure-pipelines/templates/python-ci.yaml
      - src/scripts/python_ci.sh

# PR validation mirrors the main branch trigger behavior.
pr:
  branches:
    include:
      - main

pool:
  vmImage: ubuntu-24.04

variables:
  - group: elt-ci-vars

steps:
  - template: /azure-pipelines/templates/python-ci.yaml
    parameters:
      serviceDirectory: 'src/workloads/training_pipeline'
      pythonVersion: '3.14'
      requirementsFile: 'src/workloads/training_pipeline/requirements-ci.txt'
      # The ELT workload currently uses a single requirements file. Passing the
      # same path for dev requirements is fine; the script deduplicates it so
      # pip does not reinstall the same file twice.
      devRequirementsFile: 'src/workloads/training_pipeline/requirements-ci.txt'
      includeDocker: false