trigger: none

resources:
  pipelines:
    - pipeline: ciTerraform
      source: ci-terraform
      trigger: none

pool:
  vmImage: ubuntu-24.04

jobs:
  - deployment: apply_terraform
    displayName: 'Apply Terraform'
    environment: production
    strategy:
      runOnce:
        deploy:
          steps:
            - checkout: self
              clean: true
              fetchDepth: 1

            - download: ciTerraform
              artifact: terraform-plan
              displayName: 'Download Terraform plan'

            - task: AzureCLI@2
              displayName: 'Apply Terraform plan'
              inputs:
                azureSubscription: 'azdo-oidc-cd'
                addSpnToEnvironment: true
                scriptType: bash
                scriptLocation: inlineScript
                inlineScript: |
                  set -euo pipefail
                  cd "$(Build.SourcesDirectory)/src/terraform/main"
                  chmod +x ./run.sh

                  export ARM_CLIENT_ID="${servicePrincipalId}"
                  export ARM_TENANT_ID="${tenantId}"
                  export ARM_OIDC_TOKEN="${idToken}"
                  export ARM_SUBSCRIPTION_ID="$(az account show --query id -o tsv)"
                  export ARM_USE_OIDC="true"
                  export TF_IN_AUTOMATION="true"
                  export TF_INPUT="false"

                  ./run.sh --apply-plan "$(Pipeline.Workspace)/ciTerraform/terraform-plan/prod.tfplan" --env prod